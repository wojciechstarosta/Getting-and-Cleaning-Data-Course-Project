# Getting-and-Cleaning-Data-Course-Project

Script uses 3 working directories, one for labels data set, one for train data set and one for test data set.
Beside libraries builded in R You'll need to use plyr package.
Final data set is uploaded to first directory listed above (but you can change it to anything You want).
